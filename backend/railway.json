{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install --legacy-peer-deps && npm run switch:prod && npx prisma generate && npm run build"
    },
    "deploy": {
        "startCommand": "npx prisma db push && node dist/seed.js && npm start",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}