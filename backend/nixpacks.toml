[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run switch:prod', 'npx prisma generate', 'npm run build']

[start]
cmd = 'npx prisma migrate deploy && npm start'
